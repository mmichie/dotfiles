hgrep() {
    if [[ $# -eq 0 ]]; then
        echo "Usage: hgrep <pattern>"
        return 1
    fi
    # Join into ONE pattern ($*): "$@" splatted each word separately, and
    # grep took word 2+ as FILENAMES (recalls() had the same bug).
    fc -l 1 -1 | grep --color=auto -i -- "$*"
}
