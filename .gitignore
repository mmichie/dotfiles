# swap
[._]*.s[a-w][a-z]
[._]s[a-w][a-z]
# session
Session.vim
# temporary
.netrwhist
*~
# auto-generated tag files
tags
undo*
.DS_Store

# Platform symlinks in bin/
bin/bin/fzf
bin/bin/intu
bin/bin/gam7/

# Claude-specific files
.claude/
!claude/.claude/
*.claudecontext
.claude-cache/
.claude-session/

# Personal shell configuration with secrets (lives in ~/, not in repo)
.zshrc.local

# Compiled zsh files (zcompile output)
*.zwc

# Nix
result
result-*

# Secrets backup from `just secrets-backup` — must never be committed
backup.tar.gz

# Build artifacts
bin/wifi-location-go/build/
bin/bin/t9dev
